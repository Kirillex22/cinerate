# CineRate Backend API

Это серверная часть приложения CineRate, построенная на **FastAPI**. Она отвечает за бизнес-логику, работу с базой данных PostgreSQL и взаимодействие с внешними API (например, Kinopoisk).

## 🏗️ Архитектура (Clean Architecture)

Проект следует принципам чистой архитектуры для обеспечения независимости от фреймворков и внешних агентов.

```
backend/
├── api/               # Слой представления (UI)
│   └── fastapi/       # Роутеры и зависимости FastAPI
├── domain/            # Ядро бизнес-логики (Самый важный слой)
│   ├── entities/      # Бизнес-сущности (User, Film, Playlist)
│   └── policies/      # Интерфейсы репозиториев и бизнес-правила
├── infrastructure/    # Слой инфраструктуры (всё внешнее)
│   ├── factories/     # Фабрики для создания сервисов/репозиториев
│   ├── repositories/  # Реализации интерфейсов репозиториев (PostgreSQL)
│   └── ...
├── services/          # Слой приложения (Use Cases)
│   ├── auth/          # Сценарии: регистрация, логин
│   ├── film/          # Сценарии: поиск, добавление фильмов
│   └── playlist/      # Сценарии: создание, редактирование плейлистов
├── shared/            # Общие утилиты и исключения
│   ├── exceptions/    # Кастомные типы ошибок
│   ├── mappers/       # Мапперы между сущностями и моделями БД/API
│   └── tools/         # API-клиенты, хелперы
└── web/               # Модели для API (Pydantic)
    ├── models/        # Схемы запросов и ответов
    └── ...
```

## 🚀 Локальный запуск (для разработки)

### Предварительные требования
*   Python 3.10+
*   PostgreSQL 15+

### Инструкция

1.  **Клонируйте репозиторий и перейдите в папку `backend`:**
    ```bash
    git clone https://github.com/Kirillex22/cinerate.git
    cd cinerate/backend
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python -m venv venv
    source venv/bin/activate  # для Linux/macOS
    # venv\Scripts\activate  # для Windows
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Настройте конфигурацию:**
    *   Создайте файл `config.yaml` на основе `example_config.yaml`.
    *   Укажите корректные параметры подключения к вашей локальной PostgreSQL.
    *   Заполните `JWT_SECRET` и, при необходимости, токен для внешнего API.
    *   Создайте файл `.env` в корне бэкенда, указав путь к конфигу:
        ```dotenv
        YAML_CONFIG_PATH=./config.yaml
        ```

5.  **Подготовьте базу данных:**
    Убедитесь, что база данных с именем, указанным в `config.yaml`, создана. Миграции (Alembic) должны применяться автоматически при старте, либо их нужно запустить вручную.

6.  **Запустите сервер:**
    ```bash
    uvicorn api.main:app --reload
    ```
    Сервер будет доступен по адресу `http://localhost:8000`.

## 🔧 Конфигурация (`config.yaml`)

Файл конфигурации является основным источником настроек.

| Параметр | Описание | Пример |
| :--- | :--- | :--- |
| `APP_NAME` | Название приложения | `"CineRate API"` |
| `APP_VERSION` | Версия API | `"1.0.0"` |
| `POSTGRES_SETTINGS` | **Настройки подключения к БД** | |
| `...USERNAME` | Имя пользователя БД | `"postgres"` |
| `...PASSWORD` | Пароль пользователя | `"mysecretpassword"` |
| `...DB_NAME` | Имя базы данных | `"cinerate_db"` |
| `...HOST` | Хост БД | `"localhost"` или `"db"` (для Docker) |
| `...PORT` | Порт БД | `5432` |
| `EXTERNAL_API_SETTINGS` | **Настройки внешнего API (Kinopoisk)** | |
| `...API_BASE_URL` | Базовый URL | `"https://kinopoiskapiunofficial.tech"` |
| `...API_ACCESS_TOKEN` | Токен доступа | `"YOUR_TOKEN"` |
| `SECURITY_SETTINGS` | **Настройки безопасности** | |
| `...JWT_SECRET` | Секретный ключ для JWT | `"super-secret-key"` |
| `...ACCESS_TOKEN_EXPIRE_MINUTES` | Время жизни токена | `30` |
| `ADMIN_USER` | **Данные администратора по умолчанию** | |
| `...username` | Логин | `"admin"` |
| `...email` | Email | `"admin@example.com"` |
| `...password` | Пароль | `"admin123"` |

## 📖 Документация API

После запуска сервера документация доступна по адресам:

*   Swagger UI (для тестирования): `http://localhost:8000/docs`
*   ReDoc (для чтения): `http://localhost:8000/redoc`

## 🧪 Тестирование

Для запуска тестов используйте `pytest`:

```bash
pytest
```

## 🐳 Запуск в Docker

Для запуска только бэкенда в контейнере (в связке с БД) можно использовать docker-compose файл из корня проекта. Основной скрипт сборки находится в корневой директории.

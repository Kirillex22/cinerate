# CineRate Frontend

Клиентская часть приложения CineRate, построенная на фреймворке **Angular**. Предоставляет пользовательский интерфейс для всех функций платформы.

## 🛠️ Технологии

*   **Ядро:** Angular (версия 19)
*   **Язык:** TypeScript
*   **Стилизация:** CSS
*   **Стейт-менеджмент:** Сервисы с RxJS
*   **Сетевое взаимодействие:** Angular HttpClient с интерцепторами

## 🚀 Быстрый старт

### Предварительные требования
*   Node.js (версия 18+)
*   npm (обычно идет в комплекте с Node.js)
*   Angular CLI: `npm install -g @angular/cli`

### Инструкция

1.  **Перейдите в папку фронтенда:**
    ```bash
    cd frontend
    ```

2.  **Установите зависимости:**
    ```bash
    npm install
    ```

3.  **Настройте окружение:**
    Создайте или отредактируйте файл `src/app/models/config.ts`, указав URL для API бэкенда:
    ```typescript
    // config.ts
    export const API_URL = 'http://localhost:8000'; // URL вашего локального бэкенда
    ```

4.  **Запустите сервер разработки:**
    ```bash
    ng serve
    ```
    Приложение будет доступно по адресу `http://localhost:4200/`. Оно автоматически перезагрузится при изменении файлов.

## 📊 Компоненты и их назначение

### Основные страницы
| Компонент | Назначение |
|:----------|:-----------|
| `login-page` | Авторизация пользователей |
| `register-page` | Регистрация новых пользователей |
| `profile-page` | Просмотр и редактирование профиля |
| `film-details` | Детальная информация о фильме/сериале |
| `search-page` | Поиск фильмов (внешний и внутренний) |
| `plane-page` | Список запланированных к просмотру фильмов |
| `views-page` | Список просмотренных фильмов |
| `playlists-page` | Все плейлисты пользователя |
| `playlist-details` | Содержимое конкретного плейлиста |
| `subscriptions-page` | Подписки пользователя |
| `subcribers-page` | Подписчики пользователя |

### Модальные окна (Диалоги)
| Компонент | Назначение |
|:----------|:-----------|
| `rating-dialog` | Оценка фильма при добавлении в просмотренное |
| `playlist-simple-create-dialog` | Создание простого (статического) плейлиста |
| `playlist-generate-dialog` | Создание умного (динамического) плейлиста с фильтрами |
| `playlist-access-dialog` | Управление доступом и коллабораторами плейлиста |
| `playlist-select-dialog` | Выбор плейлиста для добавления фильма |
| `update-user-profile-dialog` | Редактирование данных профиля |

### Сервисы
| Сервис | Назначение |
|:-------|:-----------|
| `auth.service` | Логика аутентификации (логин, регистрация, logout) |
| `auth.interceptor` | Добавление JWT-токена к запросам |
| `user.service` | HTTP-запросы для работы с пользователями |
| `user-state.service` | Хранение и управление состоянием текущего пользователя |
| `film.service` | HTTP-запросы для работы с фильмами |
| `playlist.service` | HTTP-запросы для работы с плейлистами |
| `search-state.service` | Управление состоянием поиска |
| `gradient.service` | Утилита для генерации градиентов для постеров |
| `navigateTo.service` | Централизованная навигация между страницами |

### Модели данных
| Интерфейс | Описание |
|:----------|:---------|
| `user.interface.ts` | Модель пользователя |
| `film.interface.ts` | Базовая модель фильма |
| `film-details.interface.ts` | Расширенная модель фильма с деталями |
| `playlists.interface.ts` | Модель для списка плейлистов |
| `playlist-details.interface.ts` | Детальная модель плейлиста |
| `config.ts` | Конфигурационные параметры (URL API) |

## 🏗️ Сборка для продакшна

Для сборки проекта в оптимизированном виде выполните:

```bash
ng build --prod
```

Результат сборки будет находиться в папке `dist/`.

## 🔄 Взаимодействие с бэкендом

Фронтенд общается с бэкендом через REST API. Все запросы проходят через `HttpClient` с использованием `auth.interceptor` для автоматического добавления JWT-токена в заголовки авторизации.

Базовый URL API настраивается в `models/config.ts`.